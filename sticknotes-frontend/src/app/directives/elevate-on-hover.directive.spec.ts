import { ElevateOnHoverDirective } from './elevate-on-hover.directive';

describe('ElevateOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ElevateOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
